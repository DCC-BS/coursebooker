# @import(./.env.runtime.schema, enabled=$IS_RUNTIME)
# @defaultSensitive=false
# ---

# @type=enum(dev, build, ci, prod)
APP_MODE=dev

# @type=boolean
IS_RUNTIME=ifs(
    eq($APP_MODE, dev), true,
    eq($APP_MODE, prod), true,
    false
)
